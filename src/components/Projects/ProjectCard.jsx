import React from "react";

import styles from "./ProjectCard.module.css";
import { getImageUrl } from "../../utils";

export const ProjectCard = ({id, name, desc, stack, demo, source }) => {
  return (
    <div className={styles.container}>
      
    </div>
  );
};